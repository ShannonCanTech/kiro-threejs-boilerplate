import Scene from './Scene'

/**
 * App - Basic Scene Only
 * 
 * This version runs only the basic scene without navigation.
 * To use: Rename this file to App.jsx (backup the original first)
 * Or import this in main.jsx instead of App.jsx
 */
function App() {
  return <Scene />
}

export default App
